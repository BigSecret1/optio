from setuptools import setup, find_packages

setup(
    name="optio",  # Replace with your project name
    version="0.1.0",
    packages=find_packages(),  # Automatically find packages
    install_requires=[
        # List dependencies here
    ],
)
